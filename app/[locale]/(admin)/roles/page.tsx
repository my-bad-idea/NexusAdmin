import { PageContainer } from '@/components/common/PageContainer';

export default function RolesPage() {
  return (
    <PageContainer title="Roles & Permissions">
      <p style={{ color: 'var(--txt-muted)', fontSize: 'var(--text-sm)' }}>Coming soon</p>
    </PageContainer>
  );
}
